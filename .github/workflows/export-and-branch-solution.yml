name: Import LV-CP solution from Dev

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  export:
    runs-on: windows-latest

    strategy:
      matrix:
        solution:
          - LVCommissionPortal
          - LVCommissionDataflow
          - LVCommissionWebsite
          - LVCommissionPortalAutomations
          - debtlog

    steps:
      - uses: actions/checkout@v3

      - uses: microsoft/powerplatform-actions/actions-install@v1

      - name: Cleanup ZIP files
        run: |
          Get-ChildItem -Path . -Filter *.zip | Remove-Item -Force -ErrorAction SilentlyContinue

      - name: Export Unmanaged Solution
        uses: microsoft/powerplatform-actions/export-solution@v1
        with:
          environment-url: ${{ secrets.DEV_URL }}
          app-id: ${{ secrets.CLIENT_ID }}
          client-secret: ${{ secrets.CLIENT_SECRET }}
          tenant-id: ${{ secrets.TENANT_ID }}
          solution-name: ${{ matrix.solution }}
          solution-output-file: ${{ matrix.solution }}.zip
          managed: false

      - name: Unpack Solution
        uses: microsoft/powerplatform-actions/unpack-solution@v1
        with:
          solution-file: ${{ matrix.solution }}.zip
          solution-folder: solutions/${{ matrix.solution }}
          solution-type: unmanaged

      - name: Commit and Push
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add solutions/
          git commit -m "Auto-export solutions from DEV" || echo "No changes to commit"
          git push
